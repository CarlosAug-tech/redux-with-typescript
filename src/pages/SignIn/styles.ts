import styled from 'styled-components';

export const Container = styled.main`
  > form {
    display: flex;
    align-items: center;
    flex-direction: column;
    max-width: 400px;
    width: 100%;
  }
`;
